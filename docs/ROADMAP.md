# pyOS Roadmap (shipped 1.0 path)

1. Honest API + package layout
2. Heap free-list, screen scroll/cursor, stack_size
3. PIT timer, keyboard line, expanded syscalls
4. GDT/TSS, paging, usercopy
5. Processes (RR)
6. VFS + seed files + shell
7. Tests + docs + 1.0.0

Out of scope: networking, GPU, SMP, x86_64, CPython-in-kernel.
